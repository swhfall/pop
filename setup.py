from setuptools import setup, find_packages

setup(name = 'reader',
    version = '1.0.0',
    packages = find_packages(),
    description = 'fasta_reader',
    python_requires = '>=3.8'
    url = 'https://github.com/swhfall/reader.git')